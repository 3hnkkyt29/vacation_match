<div class="text-center-align">
  <div class="mb-3">
    <% if user.profile_image.present? %>
      <%= image_tag user.profile_image, size: '100x100', style: 'border-radius:50%' %>&emsp;
    <% else %>
      <%= image_tag 'noimage.png' , size: '100x100', style: 'border-radius:50%' %>&emsp;
    <% end %>
  </div>

  <div class="mb-3">
    <h4>User Name</h4>
    <h5><%= user.name %></h5>
  </div>
</div>